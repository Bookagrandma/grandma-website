<%= form_for @etablissement, :url => etablissement_missions_path(@etablissement) do |form| %>    
   <% if mission.errors.any? %>    
     <div id="error_explanation">    
       <h2><%= pluralize(mission.errors.count, "error") %> prohibited this mission from being saved:</h2>    
     
       <ul>    
       <% mission.errors.full_messages.each do |message| %>    
         <li><%= message %></li>   
       <% end %>   
       </ul>   
     </div>    
   <% end %>   
     
   <div class="field">   
     <%= f.label :title %>    
     <%= f.text_area :title, id: :mission_title %>   
   </div>    
     
   <div class="field">   
     <%= f.label :body %>   
     <%= f.text_area :body, id: :mission_body %>    
   </div>    
     
   <div class="field">   
     <%= f.label :place %>    
     <%= f.text_area :place, id: :mission_place %>    
   </div>    
     
   <div class="field">   
     <%= f.label :appointment %>    
     <%= f.text_area :appointment , id: :mission_appointment %>   
   </div   
     
 <div class="field">   
     <%= f.label :benevole_id %>    
     <%= f.text_area :benevole_id, id: :mission_benevole_id %>    
   </div>        
     
   <div class="field">   
     <%= f.label :etablissement_id %>   
     <%= f.text_area :etablissement_id, id: :mission_etablissement_id %>    
   </div>    
     
     
   <div class="actions">   
     <%= f.submit %>    
   </div>    
 <% end %> 